services:
  proof-server:
    image: midnightntwrk/proof-server:8.1.0
    command: ['midnight-proof-server']
    environment:
      XDG_CACHE_HOME: /cache
    volumes: ['./.cache/proof-server:/cache']
    ports: ['127.0.0.1:6300:6300']
